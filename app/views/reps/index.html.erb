

<h1>Listing Reps</h1>
 
<%= link_to 'New Reps', new_rep_path %> | <%= link_to 'Albums', albums_path %> | <%= link_to 'Artists', artists_path %>| <%= link_to 'New Contract', new_contracts_path %> | <%= link_to 'Tours', tours_path %><br><br>


<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Contact Number</th>
    <th>Email</th>
    <th colspan="3"></th>
  </tr>
 
  <% @artists.each do |artist| %>
    <tr>
      <td><%= rep.first_name %></td>
      <td><%= rep.last_name %> </td>
      <td><%= rep.contact_number %> </td>
      <td><%= rep.email %> </td>
      <td><%= link_to 'Show', rep_path(rep) %></td>
      <td><%= link_to 'Edit', edit_rep_path(rep) %></td>
      <td><%= link_to 'Destroy', rep_path(rep), method: :delete, data: { confirm: 'Sure?' } %></td>
    </tr>
  <% end %>

</table>